name: SSH deploy via Cloudflare Tunnel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Download cloudflared
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared

      - name: Run SSH with Service Token (no browser)
        run: |
          export TUNNEL_SERVICE_TOKEN_ID="6d1ab3afecfff4b8e6f1a48f2d65b11d.access"
          export TUNNEL_SERVICE_TOKEN_SECRET="3ef605b7198815a68a94f4ea1ebf21ec7c75773c3ed4f40248fe0d0e46fa7ee4"   
          cloudflared access ssh \
            --hostname ssh.sukundev.my.id \
            --service-token-id "$TUNNEL_SERVICE_TOKEN_ID" \
            --service-token-secret "$TUNNEL_SERVICE_TOKEN_SECRET" \
            -- ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no sukundev@ssh.sukundev.my.id \
            "echo '✅ No browser login needed!' && uptime"

